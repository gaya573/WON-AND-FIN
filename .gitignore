*.iml
.gradle/
.kotlin/
/local.properties
/.idea/
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
*.apk
*.ap_
*.aab
# Build output stays out, but the relay phones are set up by downloading the
# installer from here, so the published one is kept.
!/release/*.apk

# Release signing material — never commit.
keystore.properties
*.jks
*.keystore
/dist
